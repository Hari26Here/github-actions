name: first workflow

on: push

jobs: 
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "Welcome to github actions"

            - name: list all files
              run: ls -la

